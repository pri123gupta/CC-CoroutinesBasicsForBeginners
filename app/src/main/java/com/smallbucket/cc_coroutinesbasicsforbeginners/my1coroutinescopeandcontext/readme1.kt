/*
all the tasks are executed in sequential order on the main thread.
 Both button and counter run on same thread, sequentially. As we click button , the counter increments.
 We have added logs to confirm if both are on the same thread i.e. main thread.
 


*/
